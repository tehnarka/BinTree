#include "pch.h"
#include <iostream>

TEST(destroy_tree, TestDel) {
  EXPECT_EQ(1, 10, 25);
  EXPECT_TRUE(NULL);
}

TEST(preorder_print, Testpr) {
	EXPECT_EQ(1, 10, 25, 8);
	EXPECT_TRUE(1, 10, 25, 8);
}